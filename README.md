# VibeCheck-Kyla_Yvette
Activity 3
# VibeCheck-<yourname>

## Run locally
1. Backend:
   ```bash
   cd backend
   npm install
   node index.js
2.Frontend:
Open frontend/index.html in your browser (or use Live Server).
3.Endpoints
GET /api/fortune -> { fortune: "..." }
GET /api/joke -> { joke: "..." }
GET /api/vibe?mood=happy|tired|stressed -> { mood, emoji, message }
POST /api/smash -> increments counter { smashes: number }
GET /api/smashes -> { smashes: number }
GET /api/secret?code=411L -> secret message when code is correct

Notes:
Backend default port: 3000 (changeable via env var PORT)
Evidence screenshots in evidence/ (or in DOCX file submitted)
Evidence screenshots in evidence/ (or in DOCX file submitted)
